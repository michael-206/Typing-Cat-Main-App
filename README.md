1. Make .env file with MAIL_USER, MAIL_PASS, SQLALCHEMY_DATABASE_URI, and a SECRET_KEY for the app
2. for local testing, create a virtual environment and install all the requirements, then run python app.py