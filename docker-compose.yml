version: '3.9'
services:
  web:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./instance:/app/instance   # Persist the SQLite database
    environment:
      - FLASK_ENV=production
    restart: unless-stopped
